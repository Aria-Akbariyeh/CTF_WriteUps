

# Curl

```powershell

curl "url"
# -i: showr response header
# -v: show both request and response  
# --path-as-is : Disable  URL-decoding (include characters like /%252e%252e/ (/../))
# -O: save the file 
# -o: save the file with a given name 
```